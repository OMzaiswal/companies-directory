import { OrbitProgress } from "react-loading-indicators"

export const Loading = () => {
    return (
        <OrbitProgress variant="spokes" color="#c2cc31" size="medium" text="" textColor="" />
    )
}